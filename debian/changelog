tt-toplike (0.7.13) noble; urgency=medium

  * The Insights (default) screen now has a symbol legend (press l): chip
    portraits, cyan→red temperature/power scale, the TT-process list, kill
    keys, the fleet heatmap, and the i shortcut.

 -- Tenstorrent <software@tenstorrent.com>  Sat, 04 Jul 2026 02:00:00 +0000

tt-toplike (0.7.12) noble; urgency=medium

  * Fix: the [i] Inference Server Monitor no longer appears frozen — it now
    redraws at animation cadence (60 FPS) instead of the 10 FPS data rate. The
    serving snake gently breathes at rest and the throughput timeline samples
    on a wall-clock cadence so it spans a useful window.
  * The loading snake now coils and chases its tail while compiling/loading,
    then uncoils and straightens into the ready box as the load resolves.
  * The [i] view has a proper explain overlay (rewritten for the unified
    snake) and a symbol legend (press l).

 -- Tenstorrent <software@tenstorrent.com>  Sat, 04 Jul 2026 01:00:00 +0000

tt-toplike (0.7.11) noble; urgency=medium

  * Serving view is now a full adaptive dashboard: a throughput timeline
    (scrolling tok/s + request sparklines), the token-exhaust snake, request
    swimlanes (queue→prefill→decode, from average per-stage vLLM latencies),
    a live stats panel (throughput/cache/latency/container), and a TT silicon
    strip correlating serving activity with real per-chip power/temp/AICLK.
    Regions collapse gracefully on smaller terminals.
  * vLLM /metrics parsing extended: per-stage latency, inter-token time,
    prefix-cache hit rate, preemptions.
  * Loading sequence reskinned with ANSI-art shading: block-ramp gradients and
    a compile→load→ready teal→gold hue sweep with a bright leading edge.

 -- Tenstorrent <software@tenstorrent.com>  Fri, 03 Jul 2026 23:00:00 +0000

tt-toplike (0.7.10) noble; urgency=medium

  * Serving view: the snake now sits beside a verbose, grouped info panel
    (throughput: decode/prefill tok/s · requests: running/queued/served/errors
    · cache·latency: KV% / TTFT · container: CPU/RSS/token totals), replacing
    the single-line footer. Falls back to snake-only on narrow terminals; shows
    em-dash placeholders when a server exposes no vLLM metrics.

 -- Tenstorrent <software@tenstorrent.com>  Fri, 03 Jul 2026 21:00:00 +0000

tt-toplike (0.7.9) noble; urgency=medium

  * Fix: the serving-snake stats line (tok/s · running · queued · KV · TTFT ·
    served) was rendered on the absolute bottom row and painted over by the
    global status bar, so it never appeared. The snake view now reserves that
    row for the status bar; its footer stats are visible just above it.

 -- Tenstorrent <software@tenstorrent.com>  Fri, 03 Jul 2026 20:00:00 +0000

tt-toplike (0.7.8) noble; urgency=medium

  * The [i] Inference Server Monitor is now one unified "snake" across all
    three states, replacing the Down-roster list:
    - cold  → a hungry snake roams the model-catalog starfield;
    - loading → the snake grows through the compile→load journey (gold burst
      at ready);
    - serving → a calm living creature whose form encodes live vLLM signals
      directly (body length = in-flight requests, wave = tokens/s, colour =
      KV-cache load, a pulse per completed request, a fray on errors), with a
      stats line: tok/s · running · queued · KV% · TTFT · served. Near-still
      when idle.
  * New: scrape the vLLM Prometheus /metrics endpoint per tick and fold live
    ServingStats (rates from counter deltas) onto each inference service.

 -- Tenstorrent <software@tenstorrent.com>  Fri, 03 Jul 2026 18:00:00 +0000

tt-toplike (0.7.7) noble; urgency=medium

  * Fix: the [i] Inference Server Monitor no longer sits on the cold-trail
    starfield while a tt-inference-server container is actually running. The
    monitor now tracks ANY detected container, not just the curated media
    models: generic vLLM/LLM deployments (arbitrary `--model`, e.g.
    Qwen/Qwen3-32B, passed as a container arg rather than a MODEL env var) are
    now parsed and shown, keyed by container name and labeled from the model.

 -- Tenstorrent <software@tenstorrent.com>  Fri, 03 Jul 2026 15:00:00 +0000

tt-toplike (0.7.6) noble; urgency=medium

  * Reword the process-panel empty state (TT-filtered) to "No processes
    using TT devices detected".

 -- Tenstorrent <software@tenstorrent.com>  Fri, 03 Jul 2026 13:00:00 +0000

tt-toplike (0.7.5) noble; urgency=medium

  * Inference Server Monitor [i] view is now a tri-state surface:
    - cold trail (nothing running) shows a model-catalog starfield
      screensaver: the Tenstorrent compatibility catalog as drifting stars,
      with models that run on the detected hardware lit up ("N of M models
      run on your <arch>"). Bundled snapshot + background curl refresh.
    - a compiling/loading service takes over full-screen with a boxed-snake
      progress visualization (compile -> load -> ready), filling by true %
      when a baseline exists else by live kernel/RSS momentum, and turning
      red when a load stalls (Alarm).
    - otherwise the live service list, as before.
  * Process list: outside --host mode, only processes contributing to TT
    hardware load are shown; `i` opens the Inference Server Monitor.
  * Defrag EVICT now fires on model unload / after a load surge.
  * New dependency (Recommends): curl, for the catalog refresh (HTTPS).

 -- Tenstorrent <software@tenstorrent.com>  Fri, 03 Jul 2026 12:00:00 +0000

tt-toplike (0.7.0) noble; urgency=medium

  * Animation FPS throttles: --throttle (load-based) and --idle-on-blur
    (focus-based), with /throttle and /idle-on-blur command-bar toggles and a
    status-bar hint. Both default off.
  * Apple Silicon host mode: --host now surfaces the GPU (utilisation + memory
    via ioreg) and Neural Engine (power via the private IOReport framework) as
    extra devices, no sudo required.
  * docs: mark --host and macOS/Windows builds as experimental; note the host
    mode observer effect (the visualiser shares the CPU it measures) and the
    per-hardware uniqueness of each device type, in README and project site.
  * Change: default log level is now Warn (was Info). This is global, not
    macOS-specific, so quieter startup output on Linux too; pass -v/--verbose
    for Debug-level logging, or -q/--quiet to silence entirely.

 -- Tenstorrent <software@tenstorrent.com>  Tue, 30 Jun 2026 12:00:00 +0000

tt-toplike (0.6.3) noble; urgency=medium

  * Add HostBackend (--host / --backend host): run all visualisations on
    any machine without Tenstorrent hardware. Reads CPU frequency, package
    temperature (hwmon coretemp/k10temp), and RAM usage via sysinfo + Linux
    sysfs RAPL. Maps to same Telemetry fields so Starfield, Memory Flow,
    Memory Castle, and Arcade all work unchanged.
  * Fix: default tt-toplike binary (from cargo install) now delegates to
    tt-toplike-tui instead of printing a deprecation message. Users can
    run `tt-toplike` directly after cargo install.
  * Fix: /legend, /help, /explain overlays (and l/? hotkeys) in TUI
  * Fix: Memory Flow now visualises all devices simultaneously (multi-device
    hue-band assignment; per-device particle budget).
  * docs: HOST_BACKEND.md; README "Try it on any machine" section.

 -- Tenstorrent <software@tenstorrent.com>  Mon, 16 Jun 2026 18:00:00 +0000

tt-toplike (0.6.2) noble; urgency=medium

  * Fix power reporting: HybridBackend now overlays SMBUS TDP/TDC onto sysfs
    telemetry so displayed wattage covers all power domains (Tensix + GDDR),
    matching tt-smi output. Previously the hwmon driver's vcore-only reading
    was used, which could be ~50% lower than firmware-reported total board power
    on newer firmware versions (e.g. 19.10.0).

 -- Tenstorrent <software@tenstorrent.com>  Thu, 05 Jun 2026 17:00:00 +0000

tt-toplike (0.6.1) noble; urgency=medium

  * Fix static-width number formatting: all telemetry values in arcade, TUI
    footer, memory castle, and memory flow stats bars now use minimum-width
    format specifiers (e.g. {:5.1}) so the string length never changes as
    values cross digit boundaries, eliminating layout jitter.
  * Insights compact mode: when all device panels would not fit side-by-side
    at full width (stats_w=31, gap=1), automatically switch to compact layout
    (stats_w=19, gap=0). Four Blackhole cards now fit in 151 columns instead
    of 203. Portrait art is never scaled; only the stats sidebar shrinks.
    Compact sidebar shows power bar+value, ETH count, ASIC temp, and FW
    version; GDDR temps and fan RPM are omitted in compact mode only.

 -- Tenstorrent <software@tenstorrent.com>  Mon, 01 Jun 2026 00:00:00 +0000

tt-toplike (0.5.0) noble; urgency=medium

  * Fix arcade separator borders: use unicode-width display columns instead of
    byte length so emoji labels (🏰, 🌊, ✧) don't truncate the ─ run on each
    side; odd remainders distributed right so total always fills terminal width.
  * Fix starfield centering: span-based formula (grid_rows-1)*spacing ensures
    the dot grid is centered on its geometric midpoint in all panel sizes,
    including small arcade panels where height < grid_rows.
  * Fix arcade topology: board brackets [ ] now wrap the whole board including
    the ←→ intra-board link, making multi-chip carrier boards read as
    contained units separated by ═══.
  * Add --mock N shorthand: --mock 32 now equivalent to --mock --mock-devices 32.

 -- Tenstorrent <software@tenstorrent.com>  Tue, 29 Apr 2026 00:00:00 +0000

tt-toplike (0.4.3) noble; urgency=medium

  * Fix release CI: pass -d to dpkg-buildpackage so the Build-Depends check
    is skipped when Rust toolchain is provided by rustup (dtolnay) rather
    than apt, preventing false "unmet build dependencies" failures.

 -- Tenstorrent <software@tenstorrent.com>  Fri, 24 Apr 2026 00:00:00 +0000

tt-toplike (0.4.2) noble; urgency=medium

  * CI: add release workflow (build .deb packages on v* tags, upload to GitHub
    Releases, update gh-pages version badge and download command).
  * CI: add test workflow (cargo test + zero-warnings build on PRs and main).
  * Fix dead code warnings: remove unused _in_tmux detection, unused _temp
    parameter from MemoryFlowVis constructors, and dead fetch_smbus_snapshot.

 -- Tenstorrent <software@tenstorrent.com>  Fri, 24 Apr 2026 00:00:00 +0000

tt-toplike (0.4.1) noble; urgency=medium

  * tt-toplike-app: fix VT100 pending-wrap (deferred auto-wrap) in the PTY
    screen renderer.  Characters printed at the last column now correctly defer
    the row advance until the next printable byte; escape sequences (cursor
    moves, SGR) issued between two printable characters no longer displace the
    cursor onto the wrong row, eliminating the garbled line-break artefacts.
  * Drop tt-toplike-egui package; replaced by tt-toplike-app which hosts
    tt-toplike-tui inside a PTY window (eframe) instead of reimplementing the
    visualizations in egui widgets.
  * HybridBackend: direct subprocess polling (no shell wrapper) with arc-swap
    for lock-free SMBUS snapshot delivery to the render thread.
  * EMA smoothing now handles hex-format SMBUS values ("0x10e7a", "0x3e80")
    via try_parse_numeric(); previously all hex fields were silently skipped.
  * Starfield initialised with content-area dimensions so stars are centred
    in the visible widget, not in the full terminal buffer.

 -- Tenstorrent <software@tenstorrent.com>  Thu, 17 Apr 2026 00:00:00 +0000

tt-toplike (0.4.0) noble; urgency=medium

  * HybridBackend: sysfs provides real-time core metrics (temp, power, voltage)
    while tt-smi is polled every 5 seconds in a background thread to populate
    SMBUS telemetry (DDR status, ARC health, board IDs). Zero blocking on the
    main render loop. Exposed as --backend hybrid; falls back to sysfs-only if
    tt-smi is absent.
  * BoardTopology: groups chips by SMBUS board_id (fallback to consecutive-index
    pairs when board_ids are unavailable). Exposes same_board(), board_hue(),
    board_label(). Hue palette: blue-cyan (200°), orange (20°), magenta (290°),
    green (110°).
  * sync_score(): geometric mean (a*b).sqrt() replaces abs(power_diff)/50 for
    inter-chip stream intensity. Intra-board pairs carry a 0.2 floor so links
    between on-board chips are always visible even under equal load.
  * Starfield board-aware streams: intra-board streams use ─ and board hue;
    inter-board use ═ and full rainbow sweep. Intensity driven by sync_score.
  * Memory Castle board separators: ║ in amber-gold (RGB 200,160,60) at board
    boundaries; │ in dim grey within the same board. Board label row added above
    per-device headers when ≥ 2 boards detected.
  * Arcade header topology diagram: one-line diagram shows chip blocks with
    activity bars (█▓▒░·), ←→ for intra-board links, ═══ for inter-board.
    Header expands to 4 rows when device_count ≥ 2 to accommodate diagram.
  * ArcadeVisualization.initialize_topology() wires topology into starfield and
    memory castle sub-visualizations at startup.
  * Auto-detect order updated: Hybrid → Sysfs → JSON → Mock (when both sysfs and
    tt-smi are reachable, Hybrid is preferred).

 -- Tenstorrent <software@tenstorrent.com>  Thu, 16 Apr 2026 00:00:00 +0000

tt-toplike (0.3.0) noble; urgency=medium

  * Full rainbow color cycling in Memory Castle: particle hues now sweep the
    complete 360° spectrum (was limited to 0-180°, producing only yellows and
    oranges). All four particle types (Read/Write/CacheHit/Miss) cycle together
    as a tetrad offset by 90°.
  * Background layers in Memory Castle now animate: DDR, L2, L1, and Tensix
    layer hues drift continuously rather than being hardcoded static colors.
  * Trail brightness raised from near-black (val ≤ 0.3) to visible ribbons;
    glyphs lifted from saturation 0.4/value 0.3 to 0.9/0.62 with hue animation.
  * Arcade mode @-hero now actually rendered on screen: overlay_hero() was a
    deliberate no-op (TODO comment); replaced with splice_char() implementation
    that places @ and its temperature-colored trail into the composite canvas.
  * Memory Flow DDR channel bars now grounded in SMBUS training status:
    trained channels show real current/power utilization bars, training channels
    animate with ◐◑/◒◓ spinners, untrained show dim outlines. Removed the
    synthetic frame-counter scan that implied per-channel data it didn't have.
  * Added README section: "How visualizations are grounded in hardware activity"
    covering LLM inference vs video generation patterns, idle baseline activity,
    and the tt-smi -r reset light show sequence.

 -- Tenstorrent <software@tenstorrent.com>  Wed, 16 Apr 2026 00:00:00 +0000

tt-toplike (0.1.0) noble; urgency=medium

  * Initial Debian packaging for Ubuntu 24.04 (Noble Numbat).
  * Ships two binary packages:
    - tt-toplike: TUI monitoring tool (tt-toplike-tui binary)
    - tt-toplike-egui: GPU-accelerated egui dashboard
  * Safe-by-default backend chain: sysfs → json → mock (Luwen never
    auto-detected; use --backend luwen only when hardware is idle).
  * Visualization modes: --mode arcade|castle|starfield|flow|normal
  * Recommends tt-smi (TT PPA) for JSON backend; tenstorrent-dkms for sysfs.
  * Crates vendored under vendor/ for reproducible offline builds.

 -- Tenstorrent <software@tenstorrent.com>  Thu, 10 Apr 2026 00:00:00 +0000
